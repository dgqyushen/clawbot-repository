# Test config for Si C Anode
keywords:
  primary:
    - "Si C Anode"
  secondary: []

search:
  max_results_per_keyword: 5
  sort_by: "publicationDate"
  min_year: 2023

zotero:
  target_collection: "Test Import"
  tags:
    - "test"
    - "si-c"

notification:
  # Bark device key - set via BARK_KEY environment variable or config/local.yaml
  bark_key: "${BARK_KEY}"
  bark_url: "https://bark.deepthinks.org"
  max_detail_papers: 3

api_keys:
  semantic_scholar: "${SEMANTIC_SCHOLAR_API_KEY}"
  zotero:
    library_id: "${ZOTERO_LIBRARY_ID}"
    api_key: "${ZOTERO_API_KEY}"
    library_type: "${ZOTERO_LIBRARY_TYPE:-user}"
